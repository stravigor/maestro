@layout('layouts/app')

@section('content')
  <section class="page page-sprints">
    <header class="page-header">
      <h1>Sprints</h1>
      <p>Each sprint is a column. Stories will land here.</p>
    </header>

    @if(typeof projectId !== 'undefined')
      <vue:sprint/index :project-id="projectId" />
    @else
      <vue:sprint/index />
    @end
  </section>
@end
